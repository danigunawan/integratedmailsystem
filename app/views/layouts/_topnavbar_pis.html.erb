<div class="row border-bottom white-bg">
    <nav class="navbar navbar-static-top" role="navigation">

      <div class="navbar-header" style="background: rgba(0, 0, 0, 0)">
            <button aria-controls="navbar" aria-expanded="false" data-target="#navbar" data-toggle="collapse" class="navbar-toggle collapsed" type="button">
                <i class="fa fa-reorder"></i>
            </button>

          <%= link_to landing_url do %>
            <span class="navbar-brand" style="padding: 2px; background: rgba(0, 0, 0, 0)"><%= image_tag("logo-login.png", size: "170x46")  %></span>
          <% end %>
        </div>

        <div class="navbar-collapse collapse" id="navbar">
            <ul class="nav navbar-nav">

<!--DASHBOARD              -->
              <% if can? :see, DashboardsController %>
                <li class="<%= is_active_controller('dashboards') %>">
                    <%= link_to landing_url do %>
                        <i class="fa fa-newspaper-o"></i>
                        <span class="nav-label">Dashboard</span>
                    <% end %>
                </li>
                <% end %>

<% if cannot? :manage, User %>
<!-- SURAT MASUK & Keluar            -->
              <% if can? :crud, Letter %>
              <li class="<%= is_active_controller('users') %>
                <li class="<%= is_active_controller('letters') %>">

              <%= link_to surat_url do %>
                <i class="fa fa-envelope-o"></i> <span class="nav-label">Surat</span>
              <% end %>
              </li>
              <% end %>

<!-- SURAT MASUK             -->
              <% if can? :see, Letter%>
                <% if cannot? :crud, Delegation%>
                <li class="<%= is_active_controller('users') %>
                <li class="<%= is_active_controller('letters') %>">

                <%= link_to letters_path do %>
                      <i class="fa fa-envelope-o"></i> <span class="nav-label">Surat Masuk</span>
                  <% end %>
                </li>
                <% end %>
                <% end %>


<!--DELEGASI-->
              <% if can? :crud, Delegation %>

                <li class="<%= is_active_controller('delegations') %>">
                  <%= link_to delegations_path do %>
                    <i class="fa fa-user"></i>
                    <span class="nav-label">Delegasi</span>
                  <% end %>
                </li>

              <% end %>


<!--DISPOSISI MASUK-->
              <% if can? :crud, Disposition %>
              <li class="<%= is_active_controller('dispositions') %>">
                <%= link_to dispositions_path do %>
                  <i class="fa fa-inbox"></i>
                  <span class="nav-label">Disposisi Masuk</span>
                <% end %>
              </li>
              <% end %>


<!--DISPOSISI SAYA-->
              <% if can? :see, Disposition %>
              <li class="<%= is_active_controller('mydispositions') %>">
                <%= link_to mydis_url do %>
                  <i class="fa fa-bookmark"></i>
                  <span class="nav-label">Disposisi Saya</span>
                <% end %>
              </li>
              <% end %>

<!--RIWAYAT-->
              <% if can? :see, HistoryController %>
                <li class="<%= is_active_controller('history') %>">
                <%= link_to histoo_url do %>
                  <i class="fa fa-history"></i>
                  <span class="nav-label">Riwayat</span>
                <% end %>
              </li>
              <% end %>
              <% end %>
<!--MANAGE -->
              <% if can? :manage, User%>


                    <li class="<%= is_active_controller('letters') %>">
                    <%= link_to letters_path do %>
                      <i class="fa fa-envelope-o"></i> <span class="nav-label">Surat</span>
                    <% end %>
                    </li>

                <li class="<%= is_active_controller('delegations') %>">
                  <%= link_to delegations_path do %>
                    <i class="fa fa-user"></i>
                    <span class="nav-label">Delegasi</span>
                  <% end %>
                </li>

                <li class="<%= is_active_controller('history') %>">
                      <%= link_to histoo_url do %>
                        <i class="fa fa-inbox"></i>
                        <span class="nav-label">Disposisi</span>
                      <% end %>
                    </li>

                <li class="<%= is_active_controller('mydispositions') %>">
                  <%= link_to mydis_url do %>
                    <i class="fa fa-bookmark"></i>
                    <span class="nav-label">Disposisi Saya</span>
                  <% end %>
                </li>


              <li class="dropdown">
                <a class="dropdown-toggle count-info" data-toggle="dropdown" href="#">
                  <i class="fa fa-gear"></i>
                  Manage Data
                  <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <%= link_to users_path do %>
                      User
                    <% end %>
                  </li>

                  <li>
                    <%= link_to cost_centres_path do %>
                      Cost Center
                    <% end %>
                  </li>

                  <li>
                    <%= link_to roles_path do %>
                      Role
                    <% end %>
                  </li>

                  <li>
                    <%= link_to user_activities_path do %>
                      User Activity
                    <% end %>
                  </li>

                </ul>
              </li>
<% end %>



            </ul>
            <ul class="nav navbar-top-links navbar-right">
                <li>
                    <a href="#">
                        <i class="fa fa-user"></i> <%= current_user.username unless current_user.nil? %>
                    </a>
                </li>

                <li>
                    <%= link_to logout_path, method: :delete do %>
                        <i class="fa fa-sign-out"></i> <span class="nav-label">Log Out</span>
                    <% end %>
                </li>
            </ul>
        </div>
    </nav>
</div>